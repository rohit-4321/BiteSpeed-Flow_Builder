import { CustomMessageNode } from "./CustomMessageNode";

// This include all custom nodes in flow diagram
// We will add all custom node in custom folder
export default { customNode: CustomMessageNode };
